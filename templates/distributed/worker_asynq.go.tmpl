// Code generated by go-aether {{ .AetherVersion }} on {{ .Timestamp }}. DO NOT EDIT EXCEPT IMPLEMENTATION BLOCKS.
package worker

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/hibiken/asynq"
)

// {{ .ModuleNameTitle }}Processor handles background jobs for {{ .ModuleName }}.
type {{ .ModuleNameTitle }}Processor struct {
	// TODO: Inject dependencies here (e.g. services, repositories)
}

// New{{ .ModuleNameTitle }}Processor creates a new instance of the processor.
func New{{ .ModuleNameTitle }}Processor() *{{ .ModuleNameTitle }}Processor {
	return &{{ .ModuleNameTitle }}Processor{}
}

// ProcessTask implements the asynq.Handler interface.
func (p *{{ .ModuleNameTitle }}Processor) ProcessTask(ctx context.Context, t *asynq.Task) error {
	var payload map[string]interface{}
	if err := json.Unmarshal(t.Payload(), &payload); err != nil {
		return fmt.Errorf("json.Unmarshal failed: %v: %w", err, asynq.SkipRetry)
	}

	// TODO: Implement business logic
	return nil
}
