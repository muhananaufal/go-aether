// Code generated by go-aether {{ .AetherVersion }} on {{ .Timestamp }}. DO NOT EDIT.
package event

import "context"

// Event represents a generic asynchronous message structure.
type Event struct {
	ID        string      `json:"id"`
	Type      string      `json:"type"`
	Payload   interface{} `json:"payload"`
	Timestamp string      `json:"timestamp"`
}

// Publisher defines the contract for emitting events to the message broker.
type Publisher interface {
	Publish(ctx context.Context, topic string, e Event) error
}

// Subscriber defines the contract for listening to events from the message broker.
type Subscriber interface {
	Subscribe(ctx context.Context, topic string, handler func(context.Context, Event) error) error
}
