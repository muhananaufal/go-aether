# Code generated by go-aether {{ .AetherVersion }} on {{ .Timestamp }}. DO NOT EDIT.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .PackagePath }}-deployment
  labels:
    app: aether-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: aether-app
  template:
    metadata:
      labels:
        app: aether-app
    spec:
      containers:
      - name: app
        image: your-registry/app:latest
        ports:
        - containerPort: 8080
        env:
        - name: ENV
          value: "production"
---
apiVersion: v1
kind: Service
metadata:
  name: aether-service
spec:
  selector:
    app: aether-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
