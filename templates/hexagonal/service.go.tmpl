// Code generated by go-aether {{ .AetherVersion }} on {{ .Timestamp }}. DO NOT EDIT EXCEPT IMPLEMENTATION BLOCKS.
package service

import (
	"context"
	"fmt"
	
	"{{ .PackagePath }}/{{ .Paths.Domain }}"
	"{{ .PackagePath }}/{{ .Paths.Port }}"
)

var _ port.{{ .ModuleNameTitle }}Service = (*{{ .ModuleNameTitle }}Service)(nil)

// {{ .ModuleNameTitle }}Service implements the inbound use-case orchestration for {{ .ModuleName }}.
type {{ .ModuleNameTitle }}Service struct {
	repo port.{{ .ModuleNameTitle }}Repository
}

// New{{ .ModuleNameTitle }}Service constructs the service with required dependency injection.
func New{{ .ModuleNameTitle }}Service(repo port.{{ .ModuleNameTitle }}Repository) *{{ .ModuleNameTitle }}Service {
	return &{{ .ModuleNameTitle }}Service{
		repo: repo,
	}
}

// Create handles the business logic for creating a new {{ .ModuleNameTitle }}.
func (s *{{ .ModuleNameTitle }}Service) Create(ctx context.Context, entity *domain.{{ .ModuleNameTitle }}) error {
	if err := entity.Validate(); err != nil {
		return fmt.Errorf("validation failed: %w", err)
	}
	return s.repo.Save(ctx, entity)
}

// Get retrieves a {{ .ModuleNameTitle }} by its unique identifier.
func (s *{{ .ModuleNameTitle }}Service) Get(ctx context.Context, id string) (*domain.{{ .ModuleNameTitle }}, error) {
	return s.repo.FindByID(ctx, id)
}
