# Environment Configuration for {{ .ModuleNameTitle }}

# Application Settings
APP_ENV=development
APP_PORT=8080

# Database Settings (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME={{ .ModuleName }}_db

# Add your external service keys below
# REDIS_URL=redis://localhost:6379/0
